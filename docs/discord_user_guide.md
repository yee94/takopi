# yee88 Discord Transport 用户指南

## 简介

yee88 Discord Transport 让你能够在 Discord 中与 AI 助手互动。你可以通过 Discord 消息向各种 AI 引擎（Codex, Claude, OpenCode 等）发送指令，并接收回复。

## 功能特性

- 🤖 **多引擎支持**：支持 Codex、Claude、OpenCode、Pi 等多种 AI 引擎
- 💬 **线程对话**：自动创建线程维持对话上下文
- 📝 **长消息分割**：自动处理 Discord 2000 字符限制
- 🎯 **项目绑定**：将 Discord 频道与 git 项目关联
- ⚡ **实时响应**：流式显示 AI 响应进度

## 快速开始

### 1. 安装依赖

确保已安装 yee88 和 py-cord：

```bash
# 如果你已经安装了 yee88，只需要添加 py-cord
pip install py-cord>=2.6.0

# 或者使用 uv
uv pip install py-cord>=2.6.0
```

### 2. 创建 Discord Bot

1. 访问 [Discord Developer Portal](https://discord.com/developers/applications)
2. 点击 "New Application"，输入应用名称
3. 进入 "Bot" 标签页：
   - 点击 "Reset Token" 生成 Token（**保存好这个 Token！**）
   - 启用以下 Privileged Gateway Intents：
     - ✅ Message Content Intent
     - ✅ Server Members Intent
4. 进入 "OAuth2" → "URL Generator"：
   - Scopes: 勾选 `bot` 和 `applications.commands`
   - Bot Permissions: 
     - Text: Send Messages, Read Messages/View Channels, Create Public Threads, Send Messages in Threads, Read Message History
   - 复制生成的 URL，在浏览器中打开并邀请 Bot 到你的服务器

### 3. 获取服务器和频道 ID

在配置之前，你可能需要获取服务器 ID（`guild_id`）和频道 ID（`channel_id`）：

**启用 Discord 开发者模式：**
1. 在 Discord 中打开 **用户设置** → **应用设置** → **高级**
2. 启用 **开发者模式**

**获取服务器 ID（`guild_id`）：**
- 右键点击服务器图标
- 选择 **复制服务器 ID**

**获取频道 ID（`channel_id`）：**
- 右键点击频道名称
- 选择 **复制频道 ID**

### 4. 配置 yee88

运行配置向导：

```bash
yee88 --onboard
```

按照提示输入：
1. Discord Bot Token
2. 服务器 ID（可选，用于限制 Bot 只在特定服务器工作）
3. 默认频道 ID（可选，用于发送启动消息）

或者手动编辑配置文件 `~/.config/yee88/yee88.toml`：

```toml
transport = "discord"
default_engine = "codex"  # 或其他引擎

[transports.discord]
bot_token = "YOUR_BOT_TOKEN_HERE"
guild_id = 123456789      # 可选：限制特定服务器
channel_id = 987654321    # 可选：默认启动频道
message_overflow = "split"  # 或 "trim"
session_mode = "stateless"  # 或 "chat"
show_resume_line = true
```

### 5. 启动 yee88

```bash
yee88
```

你会看到启动消息显示在配置的 Discord 频道中：

```
🐙 **yee88-discord is ready**

default: `codex`
agents: `codex, claude`
projects: `myproject, another`
working in: `/path/to/current/directory`
```

## Discord 结构映射

yee88 Discord Transport 将 Discord 的结构映射到项目/分支/会话模型：

| Discord | yee88 | 用途 |
|---------|-------|------|
| **Category** | (组织) | 视觉分组 |
| **Channel** | 项目 | 代码仓库上下文（通过 `/bind` 绑定） |
| **Thread** | 分支/会话 | 特性分支或主分支上的会话 |
| **Voice Channel** | 语音会话 | 语音与 AI 对话 |

### 结构示例

```
YEE88 (Category)
├── #main                 ← 绑定到 ~/dev/myproject
│   ├── feat/auth         ← 线程：feat/auth 分支
│   └── 修复 bug          ← 会话：master 分支
├── #another-project      ← 绑定到 ~/dev/another
└── 🔊 Voice: feat/auth   ← 语音频道绑定到 feat/auth 线程
```

## 使用方法

### 基本对话

在绑定的 Discord 频道中直接发送消息：

```
帮我写一个 Python 函数来计算斐波那契数列
```

Bot 会自动创建一个线程来回复你。

### 使用特定引擎

在消息开头加上引擎名称：

```
/claude 解释这段代码的作用
codex: 修复这个 bug
/opencode 优化这个算法
```

### 绑定项目

如果你已经注册了项目，可以使用项目别名：

```
/myproject 实现用户登录功能
```

### 在特定分支工作 (@branch)

通过在消息开头加上 `@branch-name` 在特定分支上工作：

```
@feat/new-feature 实现登录页面
@issue-123 修复这个 bug
@develop 重构数据库模型
```

这会在指定分支上创建一个新的线程。如果不加前缀，则会在主分支（如 `master`）上创建会话。

### 线程对话

Bot 会自动创建线程。在线程中回复可以：
- **维持对话上下文**：线程内的消息共享同一个会话
- **查看进度更新**：实时看到 AI 执行的操作
- **继续之前的讨论**：使用 resume token 恢复会话
- **取消任务**：进度消息上的 Cancel 按钮可以取消正在运行的任务
- **多会话并行**：不同的线程可以同时运行多个独立会话

### Slash Commands (斜杠命令)

Discord Transport 支持丰富的斜杠命令：

#### 核心命令

| 命令 | 说明 |
|------|------|
| `/bind <项目名> [worktrees_dir] [default_engine] [worktree_base]` | 将当前频道绑定到项目 |
| `/unbind` | 移除频道绑定 |
| `/status` | 显示当前频道/线程的上下文和状态 |
| `/ctx [show\|clear]` | 显示或清除上下文绑定 |
| `/cancel` | 取消正在运行的任务 |
| `/new` | 清除会话，重新开始 |

#### 引擎命令

每个配置的引擎都会自动注册为斜杠命令：

| 命令 | 说明 |
|------|------|
| `/claude [提示词]` | 向 Claude 发送消息 |
| `/codex [提示词]` | 向 Codex 发送消息 |
| `/opencode [提示词]` | 向 OpenCode 发送消息 |
| `/pi [提示词]` | 向 Pi 发送消息 |

这些命令允许你针对特定引擎，无论频道的默认引擎是什么。

#### 代理与模型命令

| 命令 | 说明 |
|------|------|
| `/agent` | 显示可用代理和当前默认设置 |
| `/model [引擎] [模型]` | 显示或设置引擎的模型覆盖 |
| `/reasoning [引擎] [level]` | 设置推理级别 (minimal/low/medium/high/xhigh) |
| `/trigger [all\|mentions\|clear]` | 设置 Bot 响应模式 |

**触发模式说明：**
- **all** (默认)：响应绑定频道/线程中的所有消息
- **mentions**：只响应 @提及或回复的消息

#### 文件传输

需要配置 `upload_dir` 才能使用文件命令：

| 命令 | 说明 |
|------|------|
| `/file get <路径>` | 从服务器下载文件或目录（打包为 zip） |
| `/file put <路径>` | 上传文件（附加文件后使用此命令回复） |

**注意：** `.git`、`.env` 和凭证文件会被阻止传输。

#### 语音功能

| 命令 | 说明 |
|------|------|
| `/voice` 或 `/vc` | 为当前线程/频道创建语音频道 |

语音频道会绑定到项目上下文，当无人时自动删除。使用本地 Whisper 进行语音转文字。

### 消息格式

**长消息处理：**
- 如果回复超过 Discord 2000 字符限制，会自动分割成多条消息
- 代码块会智能分割，确保代码格式正确

**进度显示：**
- 实时显示 AI 正在执行的操作
- 显示 elapsed time 和完成状态
- 速率限制防止 Discord API 在高活动时节流

## 配置文件详解

### 完整配置示例

```toml
transport = "discord"
default_engine = "codex"

[transports.discord]
# 必需
bot_token = "MTI4..."

# 可选
guild_id = 123456789              # 限制 Bot 只在特定服务器响应
channel_id = 987654321            # 发送启动消息的默认频道
message_overflow = "split"        # 长消息处理：split 或 trim
session_mode = "stateless"        # 对话模式：stateless 或 chat
show_resume_line = true           # 是否显示恢复会话的提示

[projects.myproject]
path = "~/projects/myproject"
default_engine = "claude"
worktree_base = "main"

[projects.another]
path = "~/projects/another"
```

### 配置选项说明

| 选项 | 必需 | 默认值 | 说明 |
|------|------|--------|------|
| `bot_token` | ✅ | - | Discord Bot Token |
| `guild_id` | ❌ | - | 限制 Bot 只在特定服务器工作 |
| `channel_id` | ❌ | - | 发送启动消息的默认频道 |
| `message_overflow` | ❌ | `"split"` | 长消息处理方式：`split` 分割或 `trim` 截断 |
| `session_mode` | ❌ | `"stateless"` | 会话模式：`stateless` 无状态或 `chat` 维持对话 |
| `show_resume_line` | ❌ | `true` | 是否显示恢复会话的提示行 |

## Bot 权限要求

邀请 Bot 时需要以下权限：

### 文本权限（必需）

| 权限 | 用途 |
|------|------|
| Read Messages / View Channels | 读取消息内容 |
| Send Messages | 发送消息 |
| Create Public Threads | 创建公开线程 |
| Send Messages in Threads | 在线程中发送消息 |
| Manage Threads | 管理线程（归档等） |
| Read Message History | 读取历史消息 |
| Add Reactions | 添加反应（Cancel 按钮） |
| Attach Files | 附加文件 |
| Use Slash Commands | 使用斜杠命令 |

### 语音权限（可选，用于 `/voice` 命令）

| 权限 | 用途 |
|------|------|
| Connect | 连接语音频道 |
| Speak | 语音说话 |
| Manage Channels | 创建/删除语音频道 |

## 故障排除

### Bot 不响应消息

1. **检查 Bot 是否在线**
   - 在 Discord 中查看 Bot 是否显示为在线状态

2. **检查权限**
   - 确保 Bot 有所需的权限（Send Messages, Read Messages, Create Threads）
   - 在 Discord 服务器设置 → 角色 → 权限中检查

3. **检查 Intents**
   - 在 Discord Developer Portal 中确认启用了 Message Content Intent

4. **检查日志**
   ```bash
   yee88 --verbose
   ```

### Token 错误

如果看到登录错误：

```
Failed to connect, check the token and try again
```

- 在 Discord Developer Portal 中重新生成 Token
- 确保复制的是完整的 Token（包括 "MTI..." 开头的部分）

### 项目绑定问题

如果使用 `@branch` 或项目别名时出错：

1. 确保项目已注册：
   ```bash
   yee88 project list
   ```

2. 注册新项目：
   ```bash
   cd ~/your-project
   yee88 project init myproject
   ```

## 最佳实践

### 1. 服务器组织建议

```
🤖 AI-Workspace (Category)
├── #general-chat        # 一般对话
├── #codex-projects      # Codex 专用频道  
├── #claude-projects     # Claude 专用频道
└── 🔊 Voice Channel     # 语音频道（如需要）
```

### 2. 使用线程保持整洁

- 每个任务创建单独线程
- 线程名称会自动反映任务内容
- 完成后可以归档线程

### 3. 敏感信息管理

**不要将 Token 提交到 git！**

```bash
# 在 .gitignore 中添加
*.toml
.env
```

将 Token 放在环境变量中：

```bash
export DISCORD_BOT_TOKEN="your-token-here"
```

### 4. 多服务器部署

如果需要 Bot 在多个服务器工作：
- 不设置 `guild_id`，Bot 会响应所有服务器
- 或使用多个 Bot Token 分别配置

## 高级用法

### 自动文件上传

将文件直接拖拽到 Discord 消息中发送，Bot 会自动：
1. 下载附件到 `upload_dir` 配置的目录
2. 将文件路径传递给 AI 引擎处理
3. 支持代码文件、文档、图片等多种格式

配置上传目录：

```toml
[transports.discord]
bot_token = "..."
upload_dir = "~/uploads"  # 或项目内的 "incoming"
```

### 配置热重载

yee88 会自动监视配置文件的变化并重新加载，无需重启 Bot：

```bash
# 修改配置文件后，设置会自动生效
yee88 config set default_engine claude
# 立即生效，无需重启
```

### 自定义系统提示

在配置中添加系统提示：

```toml
[transports.discord]
bot_token = "..."
system_prompt = """
你是我的 Discord 助手。
请用中文回复，保持简洁专业。
"""
```

### 监听特定频道

限制 Bot 只在特定频道响应：

```toml
[transports.discord]
bot_token = "..."
channel_id = 123456789  # Bot 只在这个频道响应
```

### 批量处理模式

对于大量请求，建议：
1. 使用 `/cancel` 命令取消当前任务
2. 分批发送请求
3. 使用线程隔离不同任务

### 语音对话

使用语音频道与 AI 进行语音对话：

1. 在线程中输入 `/voice` 或 `/vc`
2. Bot 会创建一个语音频道
3. 加入语音频道，使用按键说话（push-to-talk）
4. Bot 会监听语音并使用本地 Whisper 转文字
5. AI 响应后会语音回复

**语音特性：**
- 语音确认：处理前会播放确认音
- 低延迟：优化的音频处理
- 自动清理：语音频道在无人时自动删除

## 更新和维护

### 更新 yee88

```bash
pip install -U yee88
# 或
uv pip install -U yee88
```

### 查看日志

```bash
# 详细日志
yee88 --verbose

# 调试模式
yee88 --debug
```

### 重启 Bot

```bash
# 按 Ctrl+C 停止
# 然后重新运行
yee88
```

## 获得帮助

遇到问题？

1. 查看详细日志：`yee88 --verbose`
2. 检查配置文件：`cat ~/.config/yee88/yee88.toml`
3. 重新运行配置向导：`yee88 --onboard`

---

**祝你使用愉快！🐙✨**
